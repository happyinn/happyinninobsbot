package com.obsbot.happyinn.apps.happyinninobsbot.core.model.data.media

/**
 * 字体类型枚举
 * 
 * 定义了应用中可用的不同字体类型选项
 */
enum class Font {
    DEFAULT,    // 默认字体
    MONOSPACE,  // 等宽字体
    SANS_SERIF, // 无衬线字体
    SERIF,      // 衬线字体
}